import { TestBed } from '@angular/core/testing';


describe('BarReducer using testbed', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: []
    });
  });

  it('pass', () => {
    expect(true).toBe(true);
  });
});

